input {
  tcp {
    port => 5001
    mode => "server"
    codec => json_lines
  }

  udp {
    port => 5002
    codec => json_lines
  }
}

output {
  elasticsearch {
    hosts => [ "${ELASTICSEARCH_HOST}" ]
    index => "logs"
    user => "${ELASTICSEARCH_USERNAME}"
    password => "${ELASTICSEARCH_PASSWORD}"
  }
}